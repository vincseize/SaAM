@session_start();
require_once ($_SESSION['INSTALL_PATH_INC']."/checkConnect.php" );

$sequences_list = $lS->getListe(TABLE_SEQUENCES,'id,ID_project', 'id', 'ASC', 'ID_project', '=', $ID_project);

// return an Array
